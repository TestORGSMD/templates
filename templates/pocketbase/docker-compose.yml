version: "3.8"
services:
  pocketbase:
    image: spectado/pocketbase:0.23.3
    restart: unless-stopped
    volumes:
      - /etc/nexploy/templates/${HASH}/data:/pb_data
      - /etc/nexploy/templates/${HASH}/public:/pb_public
      - /etc/nexploy/templates/${HASH}/migrations:/pb_migrations
